# 🌪️ CLI Вентилятор

Интерактивное CLI приложение с анимированным вентилятором в терминале.

## 🚀 Возможности

- 🌀 Анимированный вентилятор с 4 кадрами анимации
- ⚡ Управление скоростью (1-10 уровней)
- 🎨 Цветной вывод в терминале
- 🛠️ CLI команды для управления
- ⌨️ Горячие клавиши (Ctrl+C для остановки)

## 📦 Установка

### Требования
- Go 1.22.5 или выше

### Быстрая установка
```bash
# Клонировать репозиторий
git clone <repository-url>
cd 3-course

# Установить зависимости и собрать
make deps build

# Запустить приложение
make run
```

### Установка в систему
```bash
# Собрать и установить в /usr/local/bin
make install

# Теперь можно использовать команду 'fan' из любого места
fan start
```

## 🎮 Использование

### Основные команды

```bash
# Показать справку
./fan --help

# Запустить вентилятор
./fan start

# Запустить с определенной скоростью
./fan start --speed 8

# Установить скорость (1-10)
./fan speed 7

# Остановить вентилятор
./fan stop
```

### Примеры использования

1. **Запуск вентилятора на средней скорости:**
   ```bash
   ./fan start --speed 5
   ```

2. **Изменение скорости во время работы:**
   ```bash
   ./fan speed 9
   ```

3. **Остановка вентилятора:**
   ```bash
   ./fan stop
   ```

## 🛠️ Разработка

### Доступные команды Make

```bash
make help          # Показать справку
make deps          # Установить зависимости
make build         # Собрать приложение
make run           # Запустить приложение
make install       # Установить в систему
make clean         # Очистить собранные файлы
make test          # Запустить тесты
make fmt           # Форматировать код
make lint          # Проверить код линтером
make demo          # Показать демо использования
```

### Структура проекта

```
.
├── main.go          # Основной файл приложения
├── go.mod           # Go модули и зависимости
├── Makefile         # Команды для сборки и управления
└── README.md        # Документация
```

## 🎨 Особенности

- **Анимация**: 4 кадра вращения вентилятора
- **Цвета**: Использование библиотеки `fatih/color` для красивого вывода
- **CLI**: Использование `cobra` для создания удобного интерфейса командной строки
- **Управление**: Graceful shutdown при нажатии Ctrl+C
- **Скорость**: Динамическое изменение скорости анимации

## 🔧 Технические детали

- **Язык**: Go 1.22.5
- **Зависимости**:
  - `github.com/spf13/cobra` - CLI фреймворк
  - `github.com/fatih/color` - Цветной вывод
- **Платформа**: Кроссплатформенный (Linux, macOS, Windows)

## 🎯 Скорости

| Уровень | Задержка | Описание |
|---------|----------|----------|
| 1       | 1000ms   | Очень медленно |
| 5       | 600ms    | Средняя скорость |
| 10      | 100ms    | Максимальная скорость |

## 🐛 Устранение неполадок

### Проблема: "command not found: fan"
**Решение**: Убедитесь, что приложение установлено:
```bash
make install
```

### Проблема: "вентилятор уже запущен"
**Решение**: Сначала остановите текущий вентилятор:
```bash
./fan stop
```

### Проблема: Анимация не отображается корректно
**Решение**: Убедитесь, что терминал поддерживает ANSI escape-последовательности.

## 📝 Лицензия

MIT License

## 🤝 Вклад в проект

1. Fork репозиторий
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

**Наслаждайтесь крутящимся вентилятором! 🌪️**
